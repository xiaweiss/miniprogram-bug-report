page {
  /** 最小高度填充满视口，方便设置页面背景色 */
  height: 100vh;
  /** 默认空出底部安全区 */
  border-bottom: env(safe-area-inset-bottom) solid transparent;
}

.scroll-view {
  height: 100vh;
}

.sticky-item {
  position: sticky;
  left: 0;
  top: 0;
  height: 100px;
  background: skyblue;
}

.item {
  height: 100px;
}