.page {
  display: flex;
  flex-direction: column;
  /** 最小高度填充满视口，方便设置页面背景色 */
  height: 100vh;
  /** 默认空出底部安全区 */
  border-bottom: env(safe-area-inset-bottom) solid transparent;
}

.no-bottom {
  border-bottom: none;
}
