<view class="intro">点按录音按钮，在 onStart 之前执行了 stop，录音无法停止</view>

<view class="intro">报错信息缺少了错误码
{errMsg: "operateRecorder:fail recorder not start"}</view>

<view
  class="button"
  bind:touchstart="start"
  bind:touchend="end"
  bind:touchcancel="end"
>按下录音，抬起结束</view>